const colors = require("colors")
const os = require("os");
const fs = require("fs");
const {strFunc, title} = require("./Stringfunction")
// can pull a constant straight from the other page 
// const { add } = require("./math")

// or can pull the enrire library over
// const math = require("./math")

// const {add, subtract, multiply } = require("./math")



// console.log(userDetails);

// fs.appendFile("random.txt", `Hello`, `${userDetails}`, (err) => {
//     if (err) {
//         console.log("Oops theres an error")
//     }
// });
